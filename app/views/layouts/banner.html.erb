<!DOCTYPE html>
<html>
  <head>
    <title>Rails Mock API</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="<%= asset_path('banner.css') %>">

    <%= stylesheet_link_tag "styles" %>
    <%= stylesheet_link_tag "banner" %>
  </head>

  <body>
    <button class="show-banner-button" id="showBanner">
      <i class="fas fa-arrow-right"></i>
      <span>Show product information</span>
    </button>

    <div class="join-banner" id="joinBanner">
      <button class="banner-close" aria-label="Close banner">
        <i class="fas fa-times"></i>
      </button>
      <div class="join-banner-container">
        <div class="join-banner-image">
          <%= image_tag "Leonardo_Phoenix_I_want_an_image_of_a_nested_box_that_reflects_3.jpg", alt: "Nested box reflection" %>
        </div>
        <div class="join-banner-content">
          <h1 class="join-banner-title">0xIntuition Mock API</h1>
          <p class="join-banner-description">
            This API provides an interface for creating and exposing <b>off-chain claims</b> based on the Triples and Atoms data structure of 0xIntuition.</br></br>
            It is designed to help newcomers explore the protocol's impressive ability to represent <b>rich semantic data through nested triples.</b></br></br>
            Experiment freely with <b>your own data</b> using the form below and visualize its <a href="https://graph.i7n.thp-lab.org">Graph Visualization </a> in real time:</p>
          <div class="join-banner-features">
            <div class="join-banner-feature">
              <i class="fas fa-check-circle"></i>
              <span>Atoms creation</span>
            </div>
            <div class="join-banner-feature">
              <i class="fas fa-check-circle"></i>
              <span>Seed data generation</span>
            </div>
            <div class="join-banner-feature">
              <i class="fas fa-check-circle"></i>
              <span>Triples creation</span>
            </div>
            <div class="join-banner-feature">
              <i class="fas fa-check-circle"></i>
              <span>Stress test</span>
            </div>
            <div class="join-banner-feature">
              <i class="fas fa-check-circle"></i>
              <span>Nested Triples creation</span>
            </div>
            <div class="join-banner-feature">
              <i class="fas fa-check-circle"></i>
              <span>Atoms and Triples CSV export</span>
            </div>
          </div>
          
          <a href="https://thp-lab.org/docs/intuition" class="join-banner-link">
            See the documentation <i class="fas fa-arrow-right"></i>
          </a>
        </div>
      </div>
    </div>

    <%= yield %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        const banner = document.getElementById('joinBanner');
        const closeButton = document.querySelector('.banner-close');
        const showButton = document.getElementById('showBanner');
        
        // Check if banner was previously closed
        if (localStorage.getItem('bannerClosed') === 'true') {
          banner.style.display = 'none';
          showButton.style.display = 'flex';
        } else {
          showButton.style.display = 'none';
        }

        closeButton.addEventListener('click', function() {
          banner.style.display = 'none';
          showButton.style.display = 'flex';
          localStorage.setItem('bannerClosed', 'true');
        });

        showButton.addEventListener('click', function() {
          banner.style.display = 'block';
          showButton.style.display = 'none';
          localStorage.setItem('bannerClosed', 'false');
        });
      });
    </script>
  </body>
</html>
